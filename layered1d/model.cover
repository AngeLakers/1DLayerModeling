    1: from __future__ import annotations
       
    1: from dataclasses import dataclass
    1: from typing import List, Optional, Sequence, Tuple, Dict, Any, Union
    1: import math
    1: import warnings
    1: import numpy as np
       
    1: from .materials import Material
    1: from .media import HalfSpaceMedium
       
    1: MediumLike = Union[float, HalfSpaceMedium]
       
       
    2: class Layer:
    1:     """Finite-thickness layer for normal plane longitudinal waves.
       
           Parameters
           ----------
           thickness:
               Layer thickness [m].
           density, young_modulus, poisson_ratio, attenuation_alpha, notes:
               Backward-compatible direct material properties.
           material:
               Preferred material object. When provided, ``density`` and
               legacy material fields must be omitted.
           name:
               Optional layer label. Defaults to the material name when available.
           """
       
   11:     def __init__(
               self,
    1:         thickness: float,
    2:         density: Optional[float] = None,
    2:         young_modulus: Optional[float] = None,
    2:         poisson_ratio: Optional[float] = None,
    2:         attenuation_alpha: Optional[float] = None,
    2:         notes: str = "",
    2:         name: str = "",
               *,
    2:         material: Optional[Material] = None,
    1:     ) -> None:
   17:         if not math.isfinite(thickness) or thickness <= 0:
                   raise ValueError("thickness must be positive and finite.")
       
   86:         legacy_material_fields_provided = any(
   38:             value is not None
   55:             for value in (density, young_modulus, poisson_ratio, attenuation_alpha)
    7:         ) or bool(notes)
       
   17:         if material is not None and legacy_material_fields_provided:
                   raise ValueError(
                       "Provide either material, or direct material properties "
                       "(density/young_modulus/poisson_ratio/attenuation_alpha/notes), but not both."
                   )
       
   17:         if material is None:
   10:             if density is None or young_modulus is None:
                       raise ValueError(
                           "Provide either material, or both density and young_modulus. "
                           "Preferred API: Layer(..., material=Material(...))."
                       )
   10:             if poisson_ratio is None:
    2:                 raise ValueError(
    1:                     "poisson_ratio must be provided for legacy Layer construction. "
                           "Preferred API: Layer(..., material=Material(...))."
                       )
    9:             try:
    9:                 poisson_ratio_value = float(poisson_ratio)
                   except (TypeError, ValueError):
                       raise ValueError("poisson_ratio must be finite for legacy Layer construction.") from None
    9:             if not math.isfinite(poisson_ratio_value):
    1:                 raise ValueError("poisson_ratio must be finite for legacy Layer construction.")
   16:             warnings.warn(
    8:                 "Layer(..., density=..., young_modulus=...) is supported for compatibility; "
                       "prefer Layer(..., material=Material(...)).",
    8:                 FutureWarning,
    8:                 stacklevel=2,
                   )
   16:             material = Material(
    8:                 density=float(density),
    8:                 young_modulus=float(young_modulus),
    8:                 name=name,
    8:                 poisson_ratio=poisson_ratio_value,
    8:                 attenuation_alpha=attenuation_alpha,
    8:                 notes=notes,
                   )
       
   15:         self.thickness = float(thickness)
   15:         self.material = material
   15:         self.name = name or material.name
       
    2:     @classmethod
    2:     def from_material(cls, thickness: float, material: Material, name: str = "") -> "Layer":
    7:         return cls(thickness=thickness, material=material, name=name)
       
    2:     @property
    2:     def density(self) -> float:
               return self.material.density
       
    2:     @property
    2:     def young_modulus(self) -> float:
               return self.material.young_modulus
       
    2:     @property
    2:     def poisson_ratio(self) -> float:
    1:         return self.material.poisson_ratio
       
    2:     @property
    2:     def shear_modulus(self) -> float:
    1:         return self.material.shear_modulus
       
    2:     @property
    2:     def longitudinal_modulus(self) -> float:
    2:         return self.material.longitudinal_modulus
       
    2:     @property
    2:     def attenuation_alpha(self) -> Optional[float]:
    1:         return self.material.attenuation_alpha
       
    2:     @property
    2:     def notes(self) -> str:
    1:         return self.material.notes
       
    2:     @property
    2:     def longitudinal_wave_speed(self) -> float:
               # NOTE: Delegates to material normal plane longitudinal wave speed.
   28:         return self.material.longitudinal_wave_speed
       
    2:     @property
    2:     def shear_wave_speed(self) -> float:
    2:         return self.material.shear_wave_speed
       
    2:     @property
    2:     def wave_speed(self) -> float:
               # Backward-compatible alias. Prefer longitudinal_wave_speed.
               return self.longitudinal_wave_speed
       
    2:     @property
    2:     def impedance(self) -> float:
   23:         return self.material.impedance
       
    1:     def wavenumber(self, omega: float) -> complex:
   26:         return omega / self.longitudinal_wave_speed
       
    1:     def dynamic_stiffness(self, omega: float) -> np.ndarray:
   22:         k = self.wavenumber(omega)
   22:         kh = k * self.thickness
   22:         z = self.impedance
   22:         s = np.sin(kh)
   22:         c = np.cos(kh)
       
   22:         if abs(s) < 1e-14:
    1:             kh = kh + 1e-12j
    1:             s = np.sin(kh)
    1:             c = np.cos(kh)
       
   22:         cot = c / s
   22:         csc = 1.0 / s
   22:         return omega * z * np.array([[cot, -csc], [-csc, cot]], dtype=complex)
       
    1:     def q_from_amplitudes(self, omega: float, a_plus: complex, a_minus: complex) -> np.ndarray:
    1:         k = self.wavenumber(omega)
    1:         e = np.exp(1j * k * self.thickness)
    1:         return np.array([a_plus + a_minus, a_plus * e + a_minus / e], dtype=complex)
       
    1:     def amplitudes_from_boundary_displacements(self, omega: float, u_left: complex, u_right: complex) -> Tuple[complex, complex]:
    2:         k = self.wavenumber(omega)
    2:         e = np.exp(1j * k * self.thickness)
    2:         d = np.array([[1.0, 1.0], [e, 1.0 / e]], dtype=complex)
    2:         a_plus, a_minus = np.linalg.solve(d, np.array([u_left, u_right], dtype=complex))
    2:         return a_plus, a_minus
       
    1:     def field(self, omega: float, z_local: np.ndarray, u_left: complex, u_right: complex) -> Dict[str, np.ndarray]:
    1:         a_plus, a_minus = self.amplitudes_from_boundary_displacements(omega, u_left, u_right)
    1:         k = self.wavenumber(omega)
    1:         z = self.impedance
    1:         z_local = np.asarray(z_local, dtype=float)
    1:         phase_p = np.exp(1j * k * z_local)
    1:         phase_m = np.exp(-1j * k * z_local)
    1:         u = a_plus * phase_p + a_minus * phase_m
    1:         sigma = 1j * omega * z * (a_plus * phase_p - a_minus * phase_m)
    1:         velocity = -1j * omega * u
    1:         return {
    1:             "z": z_local,
    1:             "u": u,
    1:             "sigma": sigma,
    1:             "velocity": velocity,
    1:             "a_plus": np.full_like(z_local, a_plus, dtype=complex),
    1:             "a_minus": np.full_like(z_local, a_minus, dtype=complex),
               }
       
    1:     def __repr__(self) -> str:
               return (
                   f"Layer(thickness={self.thickness!r}, material={self.material!r}, "
                   f"name={self.name!r})"
               )
       
       
    3: @dataclass(frozen=True)
    2: class InterfaceSpring:
    1:     """Zero-thickness normal spring interface with explicit finite stiffness."""
       
    1:     stiffness: float
    1:     name: str = ""
       
    1:     def __post_init__(self) -> None:
    4:         if not np.isfinite(self.stiffness):
    1:             raise ValueError("Interface stiffness must be finite.")
    3:         if self.stiffness <= 0:
    1:             raise ValueError("Interface stiffness must be positive.")
       
    1:     def dynamic_stiffness(self) -> np.ndarray:
    6:         k = float(self.stiffness)
    6:         return k * np.array([[1.0, -1.0], [-1.0, 1.0]], dtype=complex)
       
       
    3: @dataclass(frozen=True)
    2: class Connectivity:
    1:     num_dofs: int
    1:     layer_dofs: List[Tuple[int, int]]
    1:     spring_dofs: List[Tuple[int, int]]
    1:     interface_jump_nodes: List[Tuple[int, int]]
    1:     node_positions: np.ndarray
    1:     node_labels: List[str]
       
       
    2: class LaminatedStack:
    1:     """1D laminated stack with explicit zero-thickness spring interfaces."""
       
    1:     def __init__(self, layers: Sequence[Layer], interfaces: Optional[Sequence[InterfaceSpring]] = None):
    6:         if len(layers) == 0:
    1:             raise ValueError("At least one layer is required.")
    5:         self.layers: List[Layer] = list(layers)
    5:         if interfaces is None:
    3:             interfaces = []
    5:         if len(self.layers) > 1 and len(interfaces) == 0:
    1:             raise ValueError("Explicit interfaces are required when len(layers) > 1.")
    4:         if len(interfaces) != len(layers) - 1:
                   raise ValueError("interfaces length must equal len(layers) - 1.")
    4:         self.interfaces: List[InterfaceSpring] = list(interfaces)
    4:         self._connectivity = self._build_connectivity()
       
    1:     def _build_connectivity(self) -> Connectivity:
    4:         layer_dofs: List[Tuple[int, int]] = []
    4:         spring_dofs: List[Tuple[int, int]] = []
    4:         node_positions: List[float] = [0.0]
    4:         node_labels: List[str] = ["x0"]
       
    4:         current_left = 0
    4:         x = 0.0
    4:         next_node = 1
       
   10:         for j, layer in enumerate(self.layers):
    6:             if j == len(self.layers) - 1:
    4:                 right_node = next_node
    4:                 next_node += 1
    4:                 x += layer.thickness
    4:                 node_positions.append(x)
    4:                 node_labels.append(f"x{len(node_positions)-1}")
                   else:
    2:                 x += layer.thickness
    2:                 right_node = next_node
    2:                 next_node += 1
    2:                 node_positions.append(x)
    2:                 node_labels.append(f"x{len(node_positions)-1}L")
    2:                 next_left = next_node
    2:                 next_node += 1
    2:                 node_positions.append(x)
    2:                 node_labels.append(f"x{len(node_positions)-1}R")
    2:                 spring_dofs.append((right_node, next_left))
    2:                 layer_dofs.append((current_left, right_node))
    2:                 current_left = next_left
    2:                 continue
       
    4:             layer_dofs.append((current_left, right_node))
    4:             current_left = right_node
       
    8:         return Connectivity(
    4:             num_dofs=next_node,
    4:             layer_dofs=layer_dofs,
    4:             spring_dofs=spring_dofs,
    4:             interface_jump_nodes=spring_dofs,
    4:             node_positions=np.array(node_positions, dtype=float),
    4:             node_labels=node_labels,
               )
       
    2:     @property
    2:     def num_dofs(self) -> int:
   36:         return self._connectivity.num_dofs
       
    2:     @property
    2:     def node_positions(self) -> np.ndarray:
   12:         return self._connectivity.node_positions.copy()
       
    2:     @property
    2:     def node_labels(self) -> List[str]:
   12:         return list(self._connectivity.node_labels)
       
    1:     def _scatter_add_2x2(self, k_global: np.ndarray, k_local: np.ndarray, i: int, j: int) -> None:
   24:         idx = np.ix_([i, j], [i, j])
   24:         k_global[idx] += k_local
       
    1:     def assemble_structure_matrix(self, omega: float) -> np.ndarray:
   12:         k_global = np.zeros((self.num_dofs, self.num_dofs), dtype=complex)
   30:         for layer, (i, j) in zip(self.layers, self._connectivity.layer_dofs):
   18:             self._scatter_add_2x2(k_global, layer.dynamic_stiffness(omega), i, j)
   18:         for interface, dofs in zip(self.interfaces, self._connectivity.spring_dofs):
    6:             i, j = dofs
    6:             self._scatter_add_2x2(k_global, interface.dynamic_stiffness(), i, j)
   12:         return k_global
       
    5:     def _resolve_boundary_impedance(
               self,
    1:         positional_value: Optional[MediumLike],
    1:         named_value: Optional[MediumLike],
    1:         side: str,
    1:     ) -> float:
   72:         provided = [value for value in (positional_value, named_value) if value is not None]
   24:         if len(provided) != 1:
                   raise ValueError(f"Provide exactly one of {side}_medium_impedance or {side}_medium.")
   24:         value = provided[0]
   24:         if isinstance(value, HalfSpaceMedium):
   16:             return value.impedance
    8:         if not np.isfinite(value) or float(value) <= 0:
                   raise ValueError(f"{side} boundary impedance must be positive and finite.")
    8:         return float(value)
       
    7:     def _apply_boundary_conditions(
               self,
    1:         k_struct: np.ndarray,
    1:         omega: float,
    1:         left_medium_impedance: float,
    1:         right_medium_impedance: float,
    1:         incident_displacement_amplitude: complex,
    1:     ) -> Tuple[np.ndarray, np.ndarray]:
   12:         k_total = k_struct.copy()
   12:         rhs = np.zeros(self.num_dofs, dtype=complex)
   12:         k_total[0, 0] += 1j * omega * left_medium_impedance
   12:         k_total[-1, -1] += 1j * omega * right_medium_impedance
   12:         rhs[0] += 2j * omega * left_medium_impedance * incident_displacement_amplitude
   12:         return k_total, rhs
       
    6:     def _recover_scattering_outputs(
               self,
    1:         omega: float,
    1:         u: np.ndarray,
    1:         incident_displacement_amplitude: complex,
    1:         left_medium_impedance: float,
    1:     ) -> Dict[str, complex]:
   12:         u_left = u[0]
   12:         u_right = u[-1]
   12:         a_inc = incident_displacement_amplitude
   12:         a_ref = u_left - a_inc
   12:         a_tr = u_right
   12:         velocity_left = -1j * omega * u_left
   12:         force_left = 1j * omega * left_medium_impedance * u_left - 2j * omega * left_medium_impedance * a_inc
   12:         input_impedance = np.inf if abs(velocity_left) == 0 else force_left / velocity_left
   12:         return {
   12:             "reflection_coefficient": a_ref / a_inc,
   12:             "transmission_displacement_ratio": a_tr / a_inc,
   12:             "input_impedance": input_impedance,
               }
       
    1:     def _compute_interface_jumps(self, u: np.ndarray) -> np.ndarray:
   12:         interface_jumps: List[complex] = []
   18:         for dofs in self._connectivity.interface_jump_nodes:
    6:             i, j = dofs
    6:             interface_jumps.append(u[j] - u[i])
   12:         return np.array(interface_jumps, dtype=complex)
       
    9:     def solve_frequency_point(
               self,
    1:         frequency_hz: float,
    2:         left_medium_impedance: Optional[MediumLike] = None,
    2:         right_medium_impedance: Optional[MediumLike] = None,
    2:         incident_displacement_amplitude: complex = 1.0,
               *,
    2:         left_medium: Optional[MediumLike] = None,
    2:         right_medium: Optional[MediumLike] = None,
    1:     ) -> Dict[str, Any]:
   12:         omega = 2.0 * math.pi * frequency_hz
   12:         left_z = self._resolve_boundary_impedance(left_medium_impedance, left_medium, "left")
   12:         right_z = self._resolve_boundary_impedance(right_medium_impedance, right_medium, "right")
   12:         k_struct = self.assemble_structure_matrix(omega)
   24:         k_total, rhs = self._apply_boundary_conditions(
   12:             k_struct,
   12:             omega=omega,
   12:             left_medium_impedance=left_z,
   12:             right_medium_impedance=right_z,
   12:             incident_displacement_amplitude=incident_displacement_amplitude,
               )
   12:         u = np.linalg.solve(k_total, rhs)
   24:         outputs = self._recover_scattering_outputs(
   12:             omega,
   12:             u=u,
   12:             incident_displacement_amplitude=incident_displacement_amplitude,
   12:             left_medium_impedance=left_z,
               )
   36:         return {
   12:             "frequency_hz": frequency_hz,
   12:             "omega": omega,
   12:             "u": u,
   12:             **outputs,
   12:             "interface_jumps": self._compute_interface_jumps(u),
   12:             "global_matrix": k_total,
   12:             "rhs": rhs,
   12:             "node_positions": self.node_positions,
   12:             "node_labels": self.node_labels,
   12:             "left_boundary_impedance": left_z,
   12:             "right_boundary_impedance": right_z,
               }
       
    9:     def solve_sweep(
               self,
    1:         frequencies_hz: Sequence[float],
    2:         left_medium_impedance: Optional[MediumLike] = None,
    2:         right_medium_impedance: Optional[MediumLike] = None,
    2:         incident_displacement_amplitude: complex = 1.0,
               *,
    2:         left_medium: Optional[MediumLike] = None,
    2:         right_medium: Optional[MediumLike] = None,
    1:     ) -> "FrequencyResponseResult":
    5:         from .solver import FrequencyResponseResult
    5:         freqs = np.asarray(frequencies_hz, dtype=float)
   17:         solutions = [
   24:             self.solve_frequency_point(
   12:                 float(f),
   12:                 left_medium_impedance=left_medium_impedance,
   12:                 right_medium_impedance=right_medium_impedance,
   12:                 incident_displacement_amplitude=incident_displacement_amplitude,
   12:                 left_medium=left_medium,
   12:                 right_medium=right_medium,
                   )
   17:             for f in freqs
               ]
    5:         return FrequencyResponseResult.from_solutions(self, freqs, solutions)
